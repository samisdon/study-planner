export const metadata = {
  title: "AI Smart Study Planner",
  description: "Dark Mode + AI Study Generator",
};

export default function RootLayout({ children }) {
  return (
    <html lang="en">
      <body>{children}</body>
    </html>
  );
}
